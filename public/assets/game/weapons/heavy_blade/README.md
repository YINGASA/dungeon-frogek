# heavy_blade

Future heavy blade attack sheets. Recommended name: `relic_hunter_heavy_blade_attack.png`.

Use this directory for heavy blade visual attack resources only. The animation can look heavier and slower, but it must not change V1.4.2 weapon values, attack timing, hitbox size, damage, or knockback.

Naming and manifest rules should follow `docs/art-resource-pipeline.md`.
